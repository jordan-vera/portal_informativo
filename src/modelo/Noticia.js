class Noticia {
    constructor(titulo, descripcion, portada_url, tipo_noticia, archivo) {
        this.titulo = titulo;
        this.descripcion = descripcion;
        this.portada_url = portada_url;
        this.tipo_noticia = tipo_noticia;
        this.archivo = archivo;
    }
}

export default Noticia;